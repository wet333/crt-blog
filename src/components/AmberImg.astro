---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'img'> {
    src: string;
    alt: string;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={["img-amber", className]}>
    <img class="w-full h-auto" {...rest} />
</div>
